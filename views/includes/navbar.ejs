
  <style>
    body {
      /* Since a fixed navbar is removed from the document flow, add top padding */
      padding-top: 90px;
      font-family: 'Poppins', sans-serif;
      /* background-color: #f4f6f9; */
    }

    /* Navbar Styling */
    .navbar {
      position: fixed; /* Fix the navbar at the top */
      top: 0;
      left: 0;
      width: 100%;
      z-index: 1000; /* Ensure it stays on top of other content */
      /* background: linear-gradient(135deg,#3a4a58 0%,#764466 45%, #424724),
      url("https://www.transparenttextures.com/patterns/circles.png"); */
background-color: #141d2c;
opacity: 1;
background-image: linear-gradient(45deg, #363c64 50%, #1e0303 50%);
background-size: 10px 10px;
      padding: 12px 20px;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
    }

    /* Logo Styling */
    .navbar-brand img {
      height: 50px; /* Adjusted for better visibility */
      max-height: 100%;
      width: auto;
      object-fit: contain;
      transition: transform 0.3s ease-in-out;
    }

    .navbar-brand img:hover {
      transform: scale(1.1); /* Slight zoom effect on hover */
    }

    /* Shift the links a bit to the right */
    .navbar .navbar-collapse {
      margin-left: 40px; /* Adjust as needed */
    }

    /* Navbar Links */
    .navbar-nav .nav-link {
      color: #ffffff !important;
      font-weight: 700;
      font-size: 150%;
      padding: 10px 15px;
      transition: color 0.3s ease, transform 0.2s ease;
    }

    .navbar-nav .nav-link:hover {
      color: #ffcc00 !important;
      transform: translateY(-2px);
    }

    /* Active Link: Only the link for the current page should have this styling */
    .navbar-nav .nav-link.active {
      color: #ffcc00 !important;
      border-bottom: 2px solid #ffcc00;
    }

    /* Dropdown Menu */
    .dropdown-menu {
      background-color: #2c3e50;
      border-radius: 8px;
      border: none;
      padding: 10px 0;
    }

    .dropdown-item {
      color: #ffffff !important;
      font-weight: 500;
      padding: 8px 15px;
      transition: background 0.3s ease, color 0.3s ease;
    }

    .dropdown-item:hover {
      background: #ffcc00;
      color: #000000 !important;
    }

    /* Search Form */
     .navbar .form-control {
      width: 60%;
      max-width: 300px;
      border-radius: 25px;
      border: none;
      padding: 10px 15px;
    }
/*
    .navbar .btn-outline-light {
      border-radius: 25px;
      padding: 10px 20px;
      border: 2px solid #ffcc00;
      color: #ffcc00 !important;
      transition: all 0.3s ease;
    }
*/
.navbar form.d-flex {
    margin-left: -500px; /* Adjust as necessary */
  }

    .navbar .btn-outline-light:hover {
      background-color: #ffcc00;
      color: #000 !important;
    } 

    /* Login/Logout Buttons */
    .navbar-nav .login-logout .nav-link {
      padding: 10px 20px;
      border-radius: 20px;
      background-color: transparent;
      border: 2px solid #ffcc00;
      margin-left: 15px;
      transition: all 0.3s ease;
    }

    .navbar-nav .login-logout .nav-link:hover {
      background-color: #ffcc00;
      color: #000 !important;
    }

    /* Navbar Toggler */
    .navbar-toggler {
      border: none;
    }

    .navbar-toggler:focus {
      outline: none;
      box-shadow: none;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .navbar .form-control {
        width: 90%;
      }

      .navbar-nav .nav-link {
        text-align: center;
        padding: 10px;
      }

      .navbar-nav .login-logout {
        text-align: center;
        margin-top: 10px;
      }

      .navbar-nav .login-logout .nav-link {
        display: block;
        width: 100%;
      }
    }
  </style>

<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <img src="https://upload.wikimedia.org/wikipedia/en/8/83/Indian_Institute_of_Information_Technology%2C_Una_logo.png" alt="Logo" height="40">
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <!-- Remove "active" from Home so it doesn't always show as active -->
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/camera">Scan</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/entryList">Entries</a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="/" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Explore
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="/list">Student List</a></li>
              <li><a class="dropdown-item" href="/entryList">Entries List</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="/">Explore college</a></li>
            </ul>
          </li>
        </ul>

        <!-- Search Form (centered) -->
        <form class="d-flex" role="search">
          <input class="form-control me-2" id="searchToday2" type="search" placeholder="Search Your Entry" aria-label="Search">
          <button class="btn btn-outline-light" type="submit">Search</button>
        </form>

        <!-- Login/Logout at right corner -->
        <ul class="navbar-nav mb-2 mb-lg-0 login-logout">
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/logout">LogOut</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>


</body>

